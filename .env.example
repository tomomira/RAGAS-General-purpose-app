# Claude API設定
# https://console.anthropic.com/ でAPIキーを取得してください
ANTHROPIC_API_KEY=your-actual-claude-api-key-here

# デバッグ設定（オプション）
DEBUG=false
LOG_LEVEL=INFO

# 注意：
# 1. このファイルを .env にコピーして実際のAPIキーを設定してください
# 2. .env ファイルには実際のAPIキーが含まれるため、絶対にGitにコミットしないでください
# 3. .gitignore により .env ファイルは自動的に除外されます